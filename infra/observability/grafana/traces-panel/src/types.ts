export interface TrajectoriesOptions {
  /** Max characters shown for the Body preview link text. */
  bodyPreviewLength: number;
  /** Number of trajectories displayed on each page. */
  pageSize: number;
}
